---
// NAVBAR — ora basata sul componente StaggeredMenu (React Bits), adattato al
// brand: stesse voci, stessa palette, e gli stessi contatti (mail + luogo +
// social) del vecchio menu. `home` decide il prefisso dei link ancora.
import SiteMenu from "./reactbits/SiteMenu.jsx";

interface Props {
  home?: boolean;
}
const { home = false } = Astro.props;
---

<SiteMenu client:load home={home} />
